# Maintainer: young <young@qeee.net>
pkgname=__PACKAGE_NAME__
pkgver=__VERSION__
pkgrel=1
pkgdesc="Lightweight dynamic DNS (DDNS) updater"
arch=('__ARCH__')
url="https://lightddns.duaky.com"
license=('GPL-2.0-only')
depends=('glibc' 'systemd')
makedepends=('go')
options=('!debug')
backup=('etc/lightddns.yaml' 'etc/lightddns.d/example.yaml' 'etc/default/lightddns')
install=lightddns.install
source=()
sha256sums=()

package() {
	cp -a "$srcdir/../pkgroot/." "$pkgdir/"
}